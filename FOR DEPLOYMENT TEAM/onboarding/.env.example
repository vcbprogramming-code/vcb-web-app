# The single Express API (api/ at the repo root), NOT Supabase.
# The browser holds no database credentials and never reaches Supabase directly
# — see TECH_STACK.md. Leave blank to call same-origin /api/* through a proxy.
VITE_API_URL=http://localhost:8080
