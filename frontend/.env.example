# Base URL of the backend API. In dev, leave as /api to use the Vite proxy.
VITE_API_BASE_URL=/api

# Supabase (used by the frontend only for session refresh / direct reads if needed).
# The anon key is safe to expose in the browser.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
