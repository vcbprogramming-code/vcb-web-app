<!-- =======================================================================
     CURRENT STATE — OVERWRITE this block, never append. Keep it accurate so a
     developer can jump in. Refresh whenever you pause or it goes stale.
     ======================================================================= -->
## Current state — updated 2026-08-28

- **Live:** production **@116** · <https://script.google.com/macros/s/AKfycby8FFhiGqjn2tSYaj8LjIPMHwBtkQk66hed7sq1q_tCFd7XhHeHef1_NTuv7qzJDIi8Dg/exec>
- **2026-08-30 (final) — docs moved in with the stack they describe.** The root still had six loose files after the split. Each was checked against what it actually documents rather than assumed to be shared, and all six turned out to be Apps Script material:
  - `SETUP.md` is entirely `clasp` deploy steps · `DESIGN.md` states in its own opening line that everything in it is *“client-side in `index.html`”* · `DATABASES.md` describes the Google Doc store · `PROJECT_SUMMARY.md` mentions Apps Script 43 times against React 6 · both `.sql` files are exports produced by `Code.js`’s Doc parser and contain **zero** React references. All six now live in `ORIGINAL CODE/`.
  - **`CHANGELOG.md` and `README.md` stay at the root, deliberately.** `README.md` has to — GitHub only renders the root one as the landing page. `CHANGELOG.md` is keyed to Apps Script **deployment numbers** (@116, @115, …) with the React port’s syncs recorded *inside* 14 of those same entries; splitting it would either duplicate entries or break a single product timeline into two false ones.
  - **Five markdown links broke and were fixed** — the moved files sit a level deeper, so `PROJECT_SUMMARY.md`’s links to `Code.js`/`index.html` and `CHANGELOG.md` all needed re-basing. One (`ARCHITECTURE_STANDARD.md`, a cross-app doc outside this repo) was already broken beforehand and is now correctly pointed and labelled.
  - **Root is down to 2 tracked files** (`README.md`, `CHANGELOG.md`) from 9. Verified after the move: React typecheck + build ✓ · `clasp status` still lists exactly the three deployed files ✓ · `_gen_preview.js` ✓ · 14/14 doc links resolve ✓.
- **2026-08-30 (later) — cleaned the tree to two stacks, and deleted the Express port.** The previous split left three implementations on the same layer, which is what made the structure misleading. Now there are exactly two, each self-contained:
  - **`ORIGINAL CODE/`** — the live Google Apps Script app. Backend `Code.js`, frontend `index.html`. **They are not in `backend/`/`frontend/` subfolders on purpose:** Apps Script projects are flat, so `clasp` needs both beside `.clasp.json` — subfolders would break `clasp push`. The split is by file and is documented in that folder’s README. `_gen_preview.js` moved *out* of `apps-script/` so a local-only tool can never be pushed by accident.
  - **`FOR DEPLOYMENT TEAM/`** — the React 18 + TypeScript app, flattened up from the old nested `react-preview/`. Frontend is `src/`; the “backend” is `src/lib/api.ts`, a typed mock, which the README now states plainly rather than implying a server exists.
  - **The Express/TypeScript port was removed.** It predated the React port (19 Jun vs 27 Jul), nothing imported it, React never used it (its mock API is self-contained), and its `index.html` had drifted to **build 26 against production’s build 34** — eight builds of rot. Keeping it only invited someone to deploy the wrong thing. Recoverable at `git show 0a6be92:"for deploy team/src/server.ts"`.
  - **Duplicate removed:** `data/sop.json` existed twice, byte-identical. The copy that fed Express is gone; `FOR DEPLOYMENT TEAM/src/data/sop.json` is now the only one. Verified by hashing every tracked file — **zero duplicate content remains**, 58 tracked files → 45.
  - **Also cleaned:** the orphaned 42 MB root `node_modules/` (Express’s), and the emptied `react-preview/`/`data/` shells. All `desktop.ini` files were already gitignored and never in the repo.
  - **Verified after the move:** React `typecheck` + `build` ✓ · `clasp status` still lists exactly the three deployed files ✓ · `node _gen_preview.js` still works from its new home ✓ · a link checker over all nine docs reports **14/14 relative links resolving** ✓.
- **2026-08-30 — repo split into two top-level folders so the deploy team can tell the live app from the ports.** No code changed; this is a move plus the path fixes it forced.
  - **`original script/apps-script/`** — the live Google Apps Script app, plain **JavaScript** (`Code.js` + `index.html` + `appsscript.json`), no build step. This is what production serves.
  - **`for deploy team/`** — the **TypeScript** ports: the Express server (`src/`, `index.html`, `data/`), its `Dockerfile`/`package.json`/`tsconfig.json`, and `react-preview/`. Not deployed anywhere yet.
  - Docs stay at the root, so GitHub still renders `README.md` on the landing page. Each folder also has its own `README.md` explaining how to run and deploy it.
  - **Moved with `git mv`**, so history follows the files — `git log --follow` still works.
  - **What the move actually broke, and was fixed:** `clasp` resolves `.clasp.json` from the working directory, so SETUP.md’s deploy commands would have pushed nothing from the repo root — they now carry an explicit `cd`. Three markdown links in PROJECT_SUMMARY.md pointed at `Code.js`/`index.html` at the root and were dead. `.gitignore`’s `flows-preview.html` rule pointed at the old path and had stopped matching. Path claims in README.md and DATABASES.md were stale. A link checker over all nine docs now reports **20/20 relative links resolving**.
  - **Verified after the move:** Express `tsc --noEmit` ✓ · React typecheck + build ✓ · `clasp status` still lists exactly the three files it pushes ✓. The Express server and Docker build keep working because their paths are relative (`__dirname/..`, `process.cwd()/data`) and the whole unit moved together.
- **@116 — the attachments rail was crushing the case detail at iPad width.** Reported as a title wrapping one word per line on an iPad Pro (1366px). Two independent causes, both from breakpoints that were never revisited after the layout went three-column:
  - **The rail stacked only below 1180px**, so at 1366px it still took a hard 238px. The shell already spends 742px on the sidebar + list, so the reading column got `1366 - 742 - 238 - 52` = **334px**. It now stacks below **1600px** — the width where the reading column still clears ~560px with the rail attached.
  - **Stacked, it is now a chip strip** rather than a wall of thumbnails: filename + a small file glyph, one line with ellipsis, in a pill. The files stay one tap away without spending the vertical space a preview grid would — which is what was actually asked for (“don’t show them, but keep them clickable somewhere else”).
  - **The body grid had a single 1100px step**, so a 1280px window spent the full 742px on the left columns and got **248px** — *worse than a 1024px window*. A wider viewport reading worse than a narrower one is the tell. Added a 1440px step (300px + 390px).
  - **Result:** 1280px goes 248px → 538px, 1366px goes **334px → 624px**, 1440px goes 408px → 698px. Above 1600px the thumbnail rail is untouched, so the desktop view is unchanged.
  - Mirrored into `react-preview/` by re-extracting `styles.css`; typecheck + build pass.
- **@115 — docs brought current; the in-app Updates list had gone stale.** `CHANGELOG`’s array in `index.html` (what users read under Settings → Updates) still ended at the share-links release and never mentioned the editor work — added three plain-language entries. Alongside it: **PROJECT_SUMMARY.md was corrupted** — the whole document had been duplicated, the first copy truncated mid-sentence inside its own §15; removed the truncated copy (nothing lost) and added **§9a** documenting that the welcome page has no admin UI and its parts come from three different places (Doc-parsed / computed / hard-coded). **DATABASES.md carried a claim that is now false** — “no `drive` scope is needed” — corrected, since `getDriveFileName()` requires `drive.readonly`. **DESIGN.md** gained the two conventions this session kept re-learning the hard way: brand vars are surfaces, so any brand-coloured *text* needs an `html.dark` override; and full-screen modal fields must be sized by flex/grid rather than a `vh`/`calc` constant, growing the *grid* row rather than converting it to a flex column. **SETUP.md** gained the current scope list and a note that the served page is hex-escaped inside `goog.script.init(...)`, so a literal `grep` against `/exec` gives false negatives. **README.md** gained a Thai section on attachments.
- **@114 — dark mode: three places painted navy text on a navy surface.** `html.dark` redefines `--brand-soft` but deliberately NOT `--brand` / `--brand-dark` / `--brand-2` (they are surface colours elsewhere), so every rule using one as a *text* colour needs its own night-mode override. Most already had one; three did not:
  - **Flow Start/End pills** — `#16284a` on `#1c2d4a`, a contrast ratio of **1.06:1**. Effectively invisible, and the one the report came in about. Now `#cdddef` (9.97:1).
  - **Related-Files rail heading** (`.d-att-h`) — `#1F3864` on `#141b26`, **1.49:1**. Now `#7fa8d6` (6.98:1).
  - **Links** (`a`, `.sm-contact`) — `--brand-2` is tuned for white and lands at **3.57:1** on the dark panel, under the 4.5 needed for body-sized text. Now `#7fa8d6` (6.98:1).
  - **Audited rather than eyeballed:** a script walked every rule setting a brand var as `color`, cross-referenced the `html.dark` overrides, and computed WCAG ratios for each pair. That is what separated the real failures from the ones already covered — `.lbl`, `.mback`, `.w-title`, `.mh-title`, `.d-hicon` and `.m-RP .mod-ico` all looked like candidates by selector text but are fine (the last wins on specificity: `html.dark .mod-ico` is (0,2,1) vs (0,2,0)). All seven checked pairs now clear 4.5:1.
- **@113 — pasting a Drive link fills the attachment name in; typing always wins.** New admin-gated `getDriveFileName(url)` opens the file read-only, returns `getName()` with the extension stripped (`.pdf` is noise under a thumbnail), and returns an empty name for anything unresolvable — a non-Drive URL, a bad id, or a file the deploying user cannot read. A failed lookup is deliberately silent: the field just stays empty and gets typed by hand, so this never produces an error to dismiss.
  - **It only ever writes into an EMPTY name field.** Existing attachments keep the label they were saved with and do not even trigger a lookup. A row is locked the moment the admin types in it (`data-named`), and the success handler re-checks the name, the lock, and that the URL has not changed while the call was in flight. The one exception is a legacy row whose stored label was only ever the bare URL — that starts empty, so re-saving the case can finally give it a real name.
  - **Fires on paste/change, not per keystroke**, so a half-typed URL costs nothing.
  - **Needed a new OAuth scope:** `drive.readonly` in `appsscript.json`. The web app is `executeAs: USER_DEPLOYING`, so it reads Drive as the owner — the same identity that already reads the diagram folder. Verified after deploy that the app still serves normally with no authorization interstitial.
  - Guard behaviour is covered by a DOM-shim test (13 assertions) kept in the session scratchpad, not the repo: empty→fills, stored label→untouched, typed name→untouched, non-Drive→no call, empty result→no error, legacy bare-URL row→fills.
- **@112 — ขั้นตอน fills the leftover height for real; the calc constant was a guess.** @111 sized it with `height:calc(100vh - 524px)`, where 524px was my arithmetic for the header, footer, padding and fixed rows. It was over-estimated, so the field came up short and left ~180px of dead space under หมายเหตุ while ขั้นตอน still scrolled internally.
  - **The browser measures it now, not me.** `.row.ta-fill` takes `flex:1 1 auto` inside the already-flex `.mf-col`, so it absorbs whatever the two fixed rows leave. No constant to keep in sync with the chrome.
  - **Done on the GRID row, which is the part that matters.** The row was already `display:grid` from `.modal .row`; it keeps that and gains `grid-template-rows:auto minmax(0,1fr)`, and the wrapper becomes a nested grid so the textarea gets a `1fr` track above its hint. Turning the row into a *flex column* instead — the @109/@110 mistake — is what inherited `align-items:start` and packed the field to its content width. A grid track stretches by default, so width is preserved for free; `align-items:stretch` is still set explicitly to override the base `.modal .row{align-items:start}`, which in a grid would otherwise pin the track to content height.
  - **Mobile reverts both grids to `display:block`.** There the body is a normally-scrolling document, so the spare height a `1fr` track would claim is zero and the field would collapse; it goes back to a fixed `min-height`.
- **@111 — reverted ขั้นตอน to a plain block row; only ปัญหา was supposed to change.** @109 and @110 both chased a bug I introduced by widening my own scope: the request was to shrink ปัญหา’s height, but I also rewrote ขั้นตอน to absorb the leftover space with `flex:1`. That turned its row into a flex container, where the base `.modal .row{align-items:start}` still applied and packed the field to its content width — so ขั้นตอน rendered visibly narrower than the fields above and below it. @110 tried to patch that with `align-items:stretch` on the inner `div`, which was the wrong element and changed nothing.
  - **The fix was to stop overriding, not to override harder.** All three long fields are plain block rows again (no `.ta-fill`, no flex on the row), so they share a width by construction rather than by a rule that has to win a specificity fight. ปัญหา is `6.5em`, หมายเหตุ `3.2em`, and ขั้นตอน takes the remainder via `height:calc(100vh - 524px)` — the 524px constant is the measured sum of header, footer, body padding, and the fixed rows, so it tracks the viewport instead of guessing: ~12 lines at 800px, ~16 at 900px, ~24 at 1080px, clamped at a 10em floor below that.
- **@110 — ขั้นตอน was narrower than every other field.** @109 made `.ta-fill > div` a flex column so ขั้นตอน could absorb the leftover height, which had a side effect: the textarea became a *flex item*, and a flex item in a column track does not stretch to full width the way a block child does — it falls back toward its intrinsic `cols` width. The base `.modal textarea{width:100%}` was already there and was not enough, because the percentage still resolves against a container whose cross-axis alignment is not `stretch`. Fixed with `align-items:stretch` on the wrapper plus an explicit `width:100%` on the field. Only `#ed_steps` sits in a `.ta-fill` row, so it was the only field affected — ปัญหา and หมายเหตุ are plain block rows and always ran full width, which is exactly why the mismatch was visible.
- **@109 — the editor now fits one screen; the vh heights were the bug.** @108 sized ปัญหา at `26vh` and ขั้นตอน at `44vh`, which cannot work: a `vh` unit knows nothing about how tall the header, footer, or the fields above it are, so ปัญหา rendered as a half-empty slab, ขั้นตอน still scrolled internally, and หมายเหตุ was pushed off the bottom edge. Replaced with a real flex layout — `.mf-body` fills exactly the space between header and footer and no longer scrolls, each column is a full-height flex stack, ปัญหา and หมายเหตุ are sized in `em` to their actual content (8.5 / 3.4 lines), and ขั้นตอน takes `flex:1` on whatever is left. Row spacing and input padding were tightened for this modal only. Budgeted at a 900px viewport: the left column fits **up to five attachments** (~729px of 753px) and ขั้นตอน gets ~16 visible lines; past five, `.mf-meta` scrolls on its own rather than breaking the page. On mobile the flex-fill is reverted to a normally-scrolling document — one narrow column cannot fit everything, and the desktop rule would collapse ขั้นตอน to its min-height.
- **@108 — the case editor is full-screen, and attachments are named in a field instead of pipe syntax.** Two complaints, one root cause: the editor was a 780px box holding four long free-text fields, so ปัญหา / ขั้นตอน / ไฟล์แนบ each got a 2–10 row textarea with its own thumbnail-sized scrollbar, and the whole modal scrolled behind them.
  - **`.modal-full` fills the viewport** — sticky `<h3>` and `.actions`, one scrolling `.mf-body` between them. The body is a two-column `.mf-grid`: short metadata (module, tags, swap, titles, reference, attachments) on the left, the long free-text fields on the right where they get real height (`ขั้นตอน` at `44vh`, `ปัญหา` at `26vh`, both still `resize:vertical`). Labels stack above their fields inside this modal — the old 120px label gutter would have squeezed the now-narrower columns. Only `#editBg` opts in via `.modal-bg.full`; the report / confirm / settings modals are untouched.
  - **Attachments are rows, not a textarea.** Each file is a name field + a URL field + a delete button, with `+ เพิ่มไฟล์แนบ` to add one. This is what makes naming discoverable — the capability was always there, but it was `Label | URL` pipe syntax typed into a blank textarea, so files got pasted as bare links and the detail rail captioned them **"เอกสารแนบ"**. That was never a rendering bug: `attachmentCardsHtml()` deliberately refuses to print a raw `/file/d/<id>/view` as a caption and falls back to the generic word.
  - **Storage is unchanged and no server code moved.** `setAttachmentRows()` / `readAttachmentRows()` replace `attachmentsFromStorage()` / `attachmentsToStorage()` and emit the same `{label,url}` objects, so `writeAttachments_` still writes one `ไฟล์แนบ: Label | URL` line per file. A row with no URL is dropped; a row with no name round-trips as `label === url`, which is precisely the input the rail already renders as "เอกสารแนบ" — so existing unnamed attachments keep working and simply show an empty name field waiting to be filled in.
  - **Mobile keeps the sheet.** `.modal-full` was already full-height there; it drops to one column, tighter padding, `height:auto` on the textareas, and the attachment row wraps to name-over-URL since three columns are unusable at phone width.

- **@104–105 — remap applied; the admin button was a mistake and was reverted.** The @103 mapping had been correct for two days but was never *applied* — the live app kept serving the previous attachment set, which read as "wrong mapping" when it was really un-applied data. @104 added a Settings button to trigger it; that was the wrong call — it put one-off maintenance plumbing into an app the whole team uses — and @105 reverted  to its committed state, leaving no trace in the UI. Only a one-line server-side fallback remains (), which is invisible to users.
  - **Applying it needs a signed-in browser and always will.**  is gated by , which reads  — that is empty for any non-browser caller, so an anonymous request gets   is not a way around it either: it needs a linked GCP project, and this script has none. That gate is the thing keeping the SOP content safe from anything that is not the owner in a browser.
  - **Result, verified against :** 30 cases, **31 attachments, all pointing at current files, zero stale**. IC-4 carries SOP-07a + SOP-07b; the deleted  and the stray  link are gone.  remains deliberately unattached (its case was deleted).
  - **Exports refreshed from the verified state:**  (30 scenarios / 23 reports) + its  mirror, and  (581 steps, 50 tags, 31 attachments).  passes.
- **@102 — saves broke again: the payload crossed CacheService's 100 KB ceiling.** Editing any case failed with *"Argument too large: value"*. `CacheService` caps a **single value at 100 KB** and the payload had reached **102,167 bytes against a 102,400 limit** — 233 bytes of headroom — once per-case attachments were added (~97.7 KB before them, which is why saving worked earlier the same day and then stopped). This is a **hard** failure, not a degraded cache: the throw propagates out of `refreshFromDoc_()` and aborts the whole mutation, so the dialog's "nothing was changed" was accurate. Fixed by chunking the cache the same way `saveToProps_` already chunks Properties — 90 KB per key via `cachePut_`/`cacheGet_`/`cacheClear_`, count under `sopChunks`, reassembled on read; round-trip verified byte-identical. A **partial** chunk expiry deliberately reads as a cache *miss* rather than returning truncated JSON, so the code falls back to Properties and re-parses. Two chunks today, ~78 KB of room before a third.
  - PropertiesService was the first suspect and was **wrong** — that store has a 500 KB budget and was never close. Checking the actual numbers ruled it out and pointed at the cache.
- **@103 — diagrams remapped after the source changed on both sides.** Re-derived from live rather than trusting the deployed map, which had gone stale:
  - **`SOP-07` was split into `07a` (Shared Tank) + `07b` (Lot Sale)**; both attach to **IC-4**, which was itself renamed *Inter-Project* → **Inter-Company Stock Control**.
  - **The "PO Receive and IC Receive" case was deleted**, orphaning `SOP-08_PO-IC-Receipt-Doc06.pdf`. Left in Drive, deliberately unattached.
  - Cases went 31 → 30, files 31 → 32, and **IC numbering shifted** (IC-3 Balance Adjustment, IC-4 Inter-Company Stock, IC-5 Material Issuance) — the previous map would have filed several diagrams under the wrong case.
  - Result: 30 cases, 31 attachments, every mapped id verified present in the current Drive folder and used at most once.
  - **Five pairings rest on inference** where filename and title differ, confirmed with the owner before deploy: SOP-11→PM-1 (*Other* vs *Income*), SOP-14→AP-2 (*Matching* vs *Mismatch*), SOP-15→GL-1 (*Aval PN* vs *B/E & PN*), SOP-22→AP-5, SOP-29→SE-3 (that case has **no English title**; matched on Thai).
  - **Applying it is a manual step:** `/exec?attachdiagrams=1&replace=1`, admin-gated so it needs a signed-in browser. Until it is run the live app keeps serving the previous attachment set — which is exactly what caused a "wrong mapping" report that was really just un-applied data.
- **@101 — diagrams repointed after a re-upload; docs/DB brought current.** The 33 PDFs were replaced with a renumbered set of **31** (one per case), created fresh in Drive, so every stored attachment URL pointed at a now-trashed file id — those still resolve while the files sit in Trash and break the moment it empties. `bulkAttachByDisplayNo()` gained a **replace mode** (`/exec?attachdiagrams=1&replace=1`) that strips existing `ไฟล์แนบ:` lines before writing, since the default append mode would have left the dead link alongside the new one. Mapping is now a clean **1:1** (31 files ↔ 31 cases); six pairings were resolved by hand where filename and title diverge: SOP-05→OF-2, SOP-08→IC-3, SOP-14→AP-2, SOP-15→GL-1, SOP-22→AP-5, SOP-29→SE-3.
  - **DB export regenerated from live.** New `scenario_attachments` table in `DATABASE_SCHEMA.sql` (link-only: label + url, no file bytes) plus an index; `DATABASE_DATA.sql` rebuilt from `?dump=1` — 31 scenarios, 583 steps, 51 extra-module tags, 35 attachments, 23 reports. The static sections (modules, process_flows, admins) come from config, not the Doc, and were left as-is.
  - **`DATABASES.md` corrected.** It still described `backupToDrive_()` as running "on every successful mutation" — untrue since @84, and the exact claim that made the save hang hard to spot. It now documents that the Doc is the backup of record, why the call was removed, and how attachments are stored.
  - **React port at parity.** `react-preview/` already was fully TypeScript (12 tsx + 6 ts, `strict: true`, zero js/jsx) — nothing to convert. What it lacked was the attachments feature, now added: `Attachment` type, `AttachmentsRail` + `ScenarioDetailWithRail`, TH/EN strings, and the rail CSS ported verbatim. `tsc --noEmit` and `npm run build` pass.
  - **Pushed to `vcb-web-app` @ `VCB-dev`** (commit `e9dc044`), into its `sop/` folder — a *separate* React mirror that had diverged from `react-preview/` (it carries `ExtraModuleChecks.tsx`, `NewScenarioModal.tsx`, `STATUS.md` that this repo does not). Copying the folder wholesale would have destroyed that work, so only the attachment changes were transplanted. This repo (`vcbprogramming-code/SOP`) has **only `main`** — there is no `dev` branch here.
- **@100 — `data/sop.json` regenerated from live; the drift explained.** The fixture was a one-time export taken **2026-07-27** and nothing ever re-synced it, so every case edit since then widened the gap. Measured before regenerating: **29 of 31 cases** had a different `displayNo`, `titleEN`, or both (Inter-Project Stock IC-1→IC-5, GL-2 Promissory Notes→Allocate Expense, and so on down the list). It nearly caused a real error — the 33 diagram attachments were almost mapped against it, which would have filed most of them under the wrong case, silently and plausibly.
  - **Not a bug, a missing link:** the live app reads the Google Doc and never touches this file. `data/sop.json` only seeds `react-preview/` and the `src/` Express port, so nothing users see was ever affected — which is exactly why the drift went unnoticed for a month.
  - **Fix:** new read-only `/exec?dump=1` returns the full live payload in `sop.json` shape. Regenerated `data/sop.json` (31 scenarios, 23 reports, all 31 with attachments) and mirrored it to `react-preview/src/data/sop.json` per PORT_NOTES step 4. Added `Attachment` to `react-preview/src/data/types.ts`; `tsc --noEmit` passes.
  - **Re-sync whenever it matters:** `curl "<exec-url>?dump=1" -o data/sop.json` then copy into `react-preview/src/data/`. Treat the fixture as a snapshot, never as truth — read `?dump=1` or `?cases=1` live before mapping anything to cases.
  - **Still stale, deliberately left:** `DATABASE_SCHEMA.sql` / `DATABASE_DATA.sql` date from @82 (2026-08-16), predate the case renames, and have no `attachments` concept at all. They are a hand-off artifact for a Postgres rewrite that has not started, with no live consumer — regenerating them is its own task, not a side effect of this one.
- **@97–99 — Faster swap, and SOP flow diagrams attached to every case.**
  - **@97 — swap was slow for a structural reason, not a tunable one.** A single swap (even PO-1 ↔ PO-2) did three expensive things: the label loop called `parseSolutionCell_` on all 31 cases just to read a `บทที่ N` reference; the 8 cells were then deep-copied child-by-child (a solution cell holds 20+ list items, each copy/remove/append its own Docs API round-trip); and `refreshFromDoc_()` re-parsed the whole document afterwards. Distance between the two cases was irrelevant — the cost was constant and large. Now the label loop uses `getCell(3).getText()` (a text match gives the same module), and the swap **relocates the two rows** via `removeChild` + `insertTableRow` instead of rebuilding their contents — one operation each, formatting carried along for free. Index arithmetic was verified against adjacent/far/reversed orderings before deploy. `replaceCellChildren_` is now unused but left defined.
  - **Still there:** `refreshFromDoc_()` after the move. Removing it would be faster still but is entangled with the unresolved cache-staleness issue, so it stays until that is understood.
  - **@98 — `/exec?cases=1`** dumps `no|displayNo|titleEN|titleTH|att=N` for every case. Added because `data/sop.json` in this repo is **badly stale** — it lists Inter-Project Stock as IC-1 (live: IC-5) and GL-2 as Promissory Notes (live: Allocate Expense). Mapping anything against that export attaches files to the wrong cases; always read live.
  - **@99 — 33 flow-diagram PDFs wired to all 31 cases.** Files already lived in the shared Drive folder `07 Diagrams` (already set to *anyone with the link*, so thumbnails render for every viewer). `DIAGRAM_MAP_` holds the displayNo → `[{label,url}]` mapping and `bulkAttachByDisplayNo()` applies it in **one** pass, appending only the `ไฟล์แนบ:` lines and refreshing the cache once — deliberately not 31 `editScenario()` calls, which would be 31 full document re-parses. It is **idempotent**: a case already carrying the URL is skipped, so re-running is a no-op. Run once via `/exec?attachdiagrams=1` (admin-gated, so it needs a signed-in browser — `curl` is anonymous and correctly refused). Verified after the run: 35 attachments across 31 cases (33 diagrams + 2 pre-existing manual ones on IC-5/GL-2); IC-2 and OF-3 intentionally carry two related diagrams each.
  - **Five mappings were resolved by hand** where the filename did not match the title well and are worth spot-checking: SOP-05→OF-2, SOP-08→IC-3, SOP-11→OF-3, SOP-24→AP-5, SOP-31→SE-3 (that case has no English title at all).
- **@93–96 — Editor UX pass.**
  - **@93 — swap target is a dropdown, not free text.** สลับตำแหน่ง was a text input the admin had to type `PO-5` into, with no validation and nothing stopping a typo. It is now a `<select>` built from the live case list (grouped by module, showing `displayNo · title`, excluding the case being edited). No server change was needed — `swapScenarioPositions` already resolved the target by display label, which is exactly what the dropdown submits.
  - **@94 — fixed column widths.** The detail pane visibly resized between cases. The outer 3-column grid (`324px 418px 1fr`) was never the cause: `.d-wrap` had `max-width` but no `width`, so a short case shrank to its content and a long one filled out. Now `width:100%` + the same max, and `.d-cols-main` is pinned rather than flexing. The rail was already rigid at `flex:0 0 238px` (`box-sizing:border-box` is global, so padding is inside that).
  - **@95 — swap button pushed outside the modal.** Introduced by @93: a `<select>` takes its intrinsic width from its longest option, so with `flex:1 1 auto` the long `PO-1 · <full title>` entries widened the row past the modal edge. Fixed with `flex:1 1 0` + `min-width:0`, truncated option text (full title on the option’s `title`), and `minmax(0,1fr)` on `.modal .row` — a plain `1fr` track still floors at min-content width, so that hardens every modal field against the same class of overflow.
  - **@96 — no more native `alert()`.** Inside the sandboxed iframe `window.alert()` renders as *“An embedded page at n-…-script.googleusercontent.com says”*, exposing the sandbox URL and reading as broken/phishy. All 11 calls now route through `showAlert()`, which reuses the existing in-app confirm dialog (built for delete for the same reason) in a single-button mode. Messages gained titles and now state the resulting **state**, not just the verb: a hard failure says nothing was changed, a partial failure says the data *is* saved and only the view is stale. `errText()` strips the raw `Exception:` prefix and caps length. TH/EN throughout.
- **@88–89 — Per-case file attachments (PDF references).** Each case study can now carry reference files. **Link-based by design:** the admin pastes a Drive (or any) URL into a new ไฟล์แนบ field in the edit modal — the app never uploads or stores the file itself. This deliberately avoids re-introducing the `drive` OAuth scope whose absence caused the @84 save hang; the files stay in Drive under the owner's own sharing control.
  - **Storage** follows the existing metadata-line convention (`หมวดเพิ่มเติม:` / `วันที่เพิ่ม:`): one `ไฟล์แนบ: Label | URL` paragraph per file at the bottom of the solution cell, parsed back by `parseSolutionCell_` into `attachments[{label,url}]`. `writeAttachments_()` is wired into **all three** write paths (`editScenario`, `createScenario`, `renumberAllSteps_`) so attachments survive every mutation. Same omitted-vs-sent semantics as `extraModules`: field absent → keep what's in the Doc, field sent (even empty) → use exactly that.
  - **Rendering:** a 200px sticky rail to the **right** of the case content, showing each PDF as a page-1 preview image rather than a link. Thumbnails come from Drive's `thumbnail?id=…&sz=w400` endpoint; `driveFileId()` handles `/file/d/`, `?id=`, `/open?id=` and Docs-style URLs, and anything non-Drive (or a thumbnail that 404s for a viewer who can't read the file) falls back to a generic file icon via an `onerror` class toggle. (Initially the rail was emitted only for cases that had files; **@90 made it unconditional** — see below.) It uses the space left over by `.d-wrap`'s `max-width:900px` cap, so the content column is unchanged. Under 1180px the rail drops below the content as a horizontal strip instead of squeezing the steps.
  - **@90 — heading is "Related Files" / "เอกสารที่เกี่ยวข้อง", and the rail renders on EVERY case**, not just ones with files, so the layout is identical case-to-case and nothing shifts when navigating. A case with no files shows a muted "No files attached" line. Exception: under 1180px the rail stacks *below* the content where there is no empty space to fill, so an empty rail is hidden there (`.d-att-rail.is-empty`).
  - **Watch-out:** a thumbnail only renders if the Drive file is readable by the *viewer* — restricted files silently show the fallback icon. Files meant for all SOP readers need "Anyone with the link" (the owner intends these to be public to the org).
- **@86–87 — Edits landed in the Doc but never surfaced in the app.** After the @84 save-hang fix the symptom changed: saving succeeded (no hang, no error) and the edit was verifiably written to the Doc, but the app kept serving the old content indefinitely — i.e. the app was stuck on a **stale cache** that survived saves. Two changes:
  - **@86 — flush Doc writes before re-parsing.** Apps Script buffers Document writes; they aren't guaranteed visible to a subsequent `DocumentApp.openById()` read until the script ends or the pending changes are flushed. Every mutation is write-then-immediately-`refreshFromDoc_()`, and `saveAndClose()`/`flush()` appeared **nowhere** in `Code.js`, so `parseDoc_()` could re-read the *pre-edit* document and cache that. `refreshFromDoc_()` now calls `DocumentApp.openById(SOP_DOC_ID).saveAndClose()` before parsing. Deliberately **not** wrapped in try/catch — a failed flush must surface as a real error rather than silently caching stale data (that swallow-everything pattern is exactly what hid the @71 Drive bug for weeks).
  - **@87 — `/exec?recache=1` recovery endpoint.** Admin-hittable URL that drops the CacheService copy *and* the chunked Properties copy, then re-parses the Doc from scratch and reports `scenarios=/reports=/updatedAt=`. Used to recover the live app from the stale-cache state (re-parsed 31 scenarios / 23 reports cleanly, and the missing edits appeared immediately). Keep it: if the app ever serves content that no longer matches the Doc, hitting this fixes it without a redeploy.
  - **Still unconfirmed:** whether the *automatic* cache refresh on save is now reliable, or whether @86's flush merely narrowed the window. The recache endpoint proved the Doc and parser were both fine and the cache was the stale layer — but the underlying reason the post-save `refreshFromDoc_()` failed to update the cache was never isolated (a throw between the Doc write and the `cache.put` would produce exactly this, while the mutation still returns success to the client). If stale content recurs, instrument the mutation path rather than re-clearing the cache.
- **@84–85 — Saves were silently hanging; fixed.** Every admin save (edit/create/swap/delete) appeared to freeze: the button spun at "กำลังบันทึก…" forever and **no error alert ever appeared**. Cause was `backupToDrive_()`, added in @71 and called from `refreshFromDoc_()` on every mutation. Its `drive` OAuth scope was declared in `appsscript.json` but never actually authorized for the *deployed web app* (granting consent by running a function in the script editor authorizes the editor session, not the deployment), so the call failed on every save — swallowed by its own `try/catch` — and left the `google.script.run` request hanging, so `withFailureHandler` never fired. Confirmed empirically: the `SOP Backups` Drive folder did not exist and zero `sop-backup-*.json` files were ever written, i.e. that backup **never once succeeded**. Fix: `backupToDrive_(json)` is no longer called from `refreshFromDoc_()` (the function itself is left defined but unused), and the unused `drive` scope was removed from `appsscript.json` so the app stops requesting a permission it doesn't use. The Doc remains the backup of record.
  - **Watch-out for re-enabling Drive backups:** a `try/catch` protects against *errors*, not against a call that stalls. If snapshotting is ever restored, the scope must be authorized against the deployment itself, and the write should not sit in the user-blocking save path.
- **@85 — Pasted LaTeX arrows normalized.** Steps pasted from a source that rendered Markdown/LaTeX (a chat reply, a Markdown preview) arrived with arrows as literal `$\rightarrow$` instead of `→`, displaying as gibberish in the case detail. `normalizePastedText()` in `index.html` now rewrites `$\rightarrow$`, bare `\rightarrow`, `-->`/`->` to `→` (and the `\leftarrow` equivalents to `←`) on save, applied to both the steps textarea (via `stepsToStorage`) and the ปัญหา/`when` field. Line structure is deliberately untouched, and the transform is idempotent and safe on ordinary text (`5-10 บาท`, `(a->b)` are left alone).
  - **Known limit:** this repairs *arrows only*. A paste that arrived with its newlines already collapsed (several numbered steps mashed into one line, e.g. `QTY of Use1213.`) cannot be recovered in code — the line breaks never reached the textarea — and still needs manual re-breaking.
- **Architecture: fully one-way, confirmed severed.** The web app is the single source of truth; admin edits/creates/swaps/deletes write to the Google Doc as a backup copy, but the app **never reads the Doc back** into itself.
  - `syncFromDoc()`, `removeSyncTrigger()`, and `installSyncTrigger()` are **deleted from `Code.js` entirely**. `refreshFromDoc_()` remains; it's the write-then-reread-your-own-write helper every create/edit/swap/delete uses, unrelated to the old Doc→app sync.
  - **There is still no real database.** Edits go to the app + a Google Doc backup copy — nothing writes to Postgres yet. `DATABASE_SCHEMA.sql`/`DATABASE_DATA.sql` (see below) are a point-in-time hand-off export, not a live backend.
- **@78–82 — Shareable direct links.** Every case study and process flow now has a **Share** button that copies a direct link (`?case=N` / `?flow=ID`) straight to that item — opening the link jumps right past the app's normal landing view into that exact case/flow. `?case=N` is validated server-side against the parsed Doc data (`doGet` in `Code.js`); `?flow=ID` can't be (process flows are static client-side data, not parsed from the Doc — see §14 in PROJECT_SUMMARY.md), so it's validated client-side instead, falling back to the normal placeholder on a bad id. The share URL itself comes from `ScriptApp.getService().getUrl()`, injected server-side into `SOP_META.appUrl` — the client can't read its own address reliably since Apps Script serves the app inside a sandboxed iframe. Two bugs fixed along the way:
  - **Clipboard could hang the whole page** — `navigator.clipboard.writeText()` sometimes never resolved *or* rejected inside the sandboxed iframe (a permission prompt stuck in limbo), so the Share button looked frozen. Fixed by racing the Clipboard API against a 600ms timeout and falling back to `execCommand('copy')` immediately if it doesn't answer (`copyText()` in `index.html`, also backported into the older `copyEmail()`).
  - **Mobile share links landed on the branch menu, not the content** — a leftover unconditional `setMobileView(null)` in the init sequence stomped the deep-link's own "jump to detail" call. Fixed by skipping that reset when the page opened via a share link.
- **@81 — Mobile layout: Share/Edit moved off the title row.** On phones, the Share and Edit buttons now sit in the sticky back-bar row (next to "← List") instead of beside the case/flow title, so a long title never has to squeeze around fixed-width buttons and wrap across 2–3 lines. Desktop is unchanged (title row still has the room). New shared markup: `.mback-row` wraps the back button + a `#mbackDetailActions` slot that `moveDetailActionsMobile()` populates on mobile only.
- **@75–77 — Column widths + doc/data refresh.** Widened the module sidebar and case-list columns (~1cm each) to reduce empty right-side space on desktop. Regenerated `DATABASE_SCHEMA.sql`/`DATABASE_DATA.sql`/`DATABASES.md` to match `apps-script/Code.js`'s current multi-depth step model (`scenario_steps.kind ∈ numbered|sub|caption` + `depth`, replacing the old `is_substep` boolean) and the Drive-backup addition — **content itself unchanged**, this was a structural/schema catch-up, not a fresh pull from the live Doc (the live `/exec` URL can't be scraped headlessly; it's served through a sandboxed iframe requiring a real logged-in browser session).
- **@70 fix:** list-card extra-module badges now always render a case's own `extraModules` tags, regardless of which module list is being viewed — previously a card only present via a tag (e.g. a PO-primary case tagged AP) flipped to show its *primary* module's badge when viewed from the tag's list, which read as a wrong/self-referential tag (e.g. "PO-1" showing a "PO" badge while browsing AP). Also added: an "Open NotebookLM" link button in the Reports view header (links to the team's NotebookLM notebook), and a parser-level fix so a case can never end up tagged with its own primary module in the first place (`apps-script/Code.js`, both the Doc-parse path and `editScenario`'s write path now filter self-matching tags, matching what `createScenario` already did).
- **Done previous cycle (2026-07):**
  - **"+ New case"** admin feature — create a case study from inside the app (no more Docs-only editing). Server assigns `no`; `createScenario()` in `Code.js`.
  - **`dateAdded`** field per case (Thai-formatted, stamped on create, shown under the reference footer).
  - **Per-module running numbers** (`displayNo`, e.g. `PO-3`) — computed fresh on every read from row order per module (`assignDisplayNo_()`), never stored as authoritative.
  - **Multi-module tagging** (`extraModules`) — a case can be tagged into other modules' lists beyond its primary one (numbering/color still come from the primary module). Tag badges live on the list cards (`.lc-badge-tag` in `index.html`), always showing the case's own tags (see @70 fix above).
  - **Swap** two cases' positions/content directly (`swapScenarioPositions()`), and **Delete** a case (`deleteScenario()`, later same-module cases renumber up) — both admin-only, Delete lives inside the Edit modal, confirmed via a custom in-app dialog (not native `confirm()`, which leaked the Apps Script iframe URL in browser chrome).
  - Case-detail header layout redesign: number badge, title, and Edit button top-align cleanly regardless of title wrapping.
  - **`DATABASE_SCHEMA.sql` + `DATABASE_DATA.sql`** added at the repo root — a PostgreSQL export of the full live content (modules, case studies + steps, reports, all 33 process flows) for handing off to a Postgres-backed rewrite. Not wired into the live app; a point-in-time export.
- **React port (`react-preview/` in this repo):** re-synced to full feature parity with `apps-script/` @70 — `displayNo`, `extraModules` badges (always-own-tags behavior), `dateAdded`, the NotebookLM report-view link, and full admin CRUD (create/edit/swap/delete a case, ported into `EditModal.tsx`/`store.tsx`/`lib/api.ts`) all added; the dead "Sync from Doc" button/state/i18n removed to match `apps-script/`. `styles.css` re-extracted verbatim from `apps-script/index.html`'s `<style>` block. `data/sop.json` (repo root, mirrored into `react-preview/src/data/`) stamped with computed `displayNo` + `dateAdded`, but `extraModules` seeded empty everywhere — the *real* live tag data lives only in the Apps Script server cache (populated from the Doc) and isn't reachable from this repo; see `react-preview/PORT_NOTES.md`'s "Known gap" note. `npm run build` + `tsc --noEmit` both pass; not verified in an actual browser this pass (no headless-browser tooling in this environment).
- **In progress / next:** nothing pending from active requests. The one open question is whether/when to actually build a Postgres-backed rewrite using the `DATABASE_SCHEMA.sql`/`DATABASE_DATA.sql` export — no work has started on that; it's still just an export sitting in the repo.
- **Watch-outs:**
  - Clipboard copy can outright hang (not just fail) inside the Apps Script sandboxed iframe — `copyText()` (used by both `shareCase`/`shareFlow` and `copyEmail`) now races a 600ms timeout against the Clipboard API and falls back to `execCommand('copy')` rather than waiting on it indefinitely. If a *new* copy-to-clipboard spot is added, route it through `copyText()`, not a fresh `navigator.clipboard.writeText()` call.
  - `@HEAD` `/exec` is NOT a reliable preview (served stale code). Preview via a throwaway dated `create-deployment`, then `delete-deployment`. See SETUP.md.
  - The VCB Connect back-link URL is **hard-coded** in `index.html` — if that portal is republished as a new deployment, update the `href`.
  - `no` (the Doc row-order id) is the only stable identifier — `displayNo` (e.g. `PO-3`) is always derived, never trust it as a lookup key server-side.
  - `react-preview/`'s mock data has no real `extraModules` tags (see above) — the badge UI is correct but has nothing to visually demo until real tag data is hand-added or a live pull is wired in.
  - **`react-preview/` is now behind `apps-script/`** — last synced at @70; the @75–82 work (column widths, share links, mobile action-bar move, clipboard fix) has **not** been ported over. Not urgent (it's a preview/reference copy, not live), but don't assume it reflects current `apps-script/` behavior.

<!-- =======================================================================
     MILESTONES — append ONE line when a real feature/polish pass lands.
     Roll small tweaks under the milestone they belong to. Never one line per deploy.
     ======================================================================= -->
## Milestones

- **2026-08-05/06** — Shareable direct links for case studies (`?case=N`) and process flows (`?flow=ID`): a Share button on each detail page copies a URL that opens straight to that item, past the app's normal landing view. Server injects the deployed `/exec` URL (`SOP_META.appUrl`) since the client can't read its own address inside the sandboxed iframe; `?case=N` is validated server-side, `?flow=ID` client-side (flows are static data, not Doc-parsed). Fixed two bugs found while building it: a clipboard-copy hang that could freeze the whole page (Clipboard API now races a timeout), and mobile share links landing on the branch menu instead of the shared content. Also moved Share/Edit off the title row on mobile (into the sticky back-bar) so long titles stop wrapping around them, and widened the desktop sidebar/list columns by ~1cm to cut down empty right-side space. Deployed as production @82. Regenerated `DATABASE_SCHEMA.sql`/`DATABASE_DATA.sql`/`DATABASES.md` to match the current multi-depth step model and Drive backup (structural catch-up only, not new content — see Watch-outs). `react-preview/` **not** re-synced this pass — still at @70 parity.
- **2026-07-27** — Fixed extra-module badges always showing the case's own tags instead of flipping to the primary module when viewed from a tagged-into list; added an "Open NotebookLM" link in the Reports view; hardened the tag parser/writer against a case ever being tagged with its own module. Deployed as production @70. Re-synced `react-preview/` to full feature parity (displayNo, extraModules badges, dateAdded, NotebookLM link, full admin CRUD), removed its dead Sync-from-Doc leftovers, and re-extracted `styles.css` verbatim.
- **2026-07-26** — Confirmed the Doc sync severance and removed the last of the dead code: verified the live trigger store is empty, deleted `syncFromDoc()`/`removeSyncTrigger()`/`installSyncTrigger()` from `Code.js` (production @69), and found + removed a live "Sync from Doc" button in the React mirror's Settings modal that an earlier docs-only pass had missed.
- **2026-07-26** — List-card tag badges: extra-module tags moved out of the detail header (was cluttering the header/breaking alignment) into the middle list — a case shows a lighter-toned badge for its non-primary module tags, visible in every list view (own module, tagged-into module, and "All"), not just when filtered to the tagged module.
- **2026-07-26** — DATABASE_SCHEMA.sql / DATABASE_DATA.sql added: full PostgreSQL export of live content (modules, 31 case studies incl. steps, 21 reports, 33 process flows) for a future Postgres-backed rewrite.
- **2026-07** — Case detail header redesign: tag chips (later moved to the list, see above), number badge, and Edit button now top-align consistently regardless of title wrapping.
- **2026-07** — One-way architecture: app is now the single source of truth; writes to the Google Doc as backup only, never reads it back. Removed the 5-min auto-sync trigger and the "⟲ Sync" button/flow.
- **2026-07** — Case management from the app: "+ New case" creation, per-module running numbers (`displayNo`), multi-module tagging (`extraModules`), swap two cases' positions, delete a case (admin-only, custom confirm dialog), `dateAdded` field.
- **2026-06-03** — Refined-corporate UI revamp: inline SVG line-icon system replaces all emoji; refined design tokens, banner, cards, focus rings, dark mode; removed an invented banner logo mark; Contact email switched to click-to-copy.
- **2026-06-03** — Navigation unified into 3 branches (Process Flows + Case Studies module submenus with root = "All"; Reports leaf), grouped flow list, rich landing page, Settings About/Contact.
- **2026-06-03** — Process Flows feature: 33 swim-lane diagrams bundled as `SOP_FLOWS` with a native HTML-grid + SVG-edge renderer.
- **2026-05-31** — Dark-mode fixes (mobile module cards, low-contrast text).
