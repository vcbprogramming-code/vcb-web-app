# The single Express API at api/ (Render). No Supabase keys here: the browser
# never talks to Supabase directly — every request goes through Express, which
# is the only place database credentials live. See TECH_STACK.md.
VITE_API_URL=http://localhost:8080
