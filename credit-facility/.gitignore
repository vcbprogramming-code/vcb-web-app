# clasp credentials — NEVER commit (contains OAuth tokens)
.clasprc.json
~/.clasprc.json

# Local backups / dependencies
remote_backup/
node_modules/

# Windows / OS noise
desktop.ini
Thumbs.db
.DS_Store
