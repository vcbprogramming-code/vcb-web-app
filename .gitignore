# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo
.vercel

# Environment / secrets — never commit
.env
.env.local
.env.*.local
!.env.example

# Google Apps Script local credentials
.clasprc.json

# Windows / Google Drive noise
desktop.ini
Thumbs.db
$RECYCLE.BIN/

# Google Drive shortcut + binary mirror files (not source)
*.gsheet
*.gscript
*.gdoc
*.url

# Editor
.vscode/
.idea/
*.swp
.DS_Store

# Logs
npm-debug.log*
yarn-error.log*
*.log

# Agent worktrees
.claude/worktrees/

# Large unreferenced media (kept on disk, not in git)
*.mp4

# ...except the login-screen background, which the portal REFERENCES. It is
# 1.9MB (the 50MB source lives in _BACKUP), and without it in the repo the
# deployment team gets a sign-in page with a dead <video> and no way to know
# what belongs there.
!FOR DEPLOYMENT TEAM/portal/public/globe.mp4

# Contains a live Google OAuth client secret - excluded until credential is moved out of source
ORIGINAL CODE/ememo/Code.js
ORIGINAL CODE/ememo/PROJECT_SUMMARY.md
