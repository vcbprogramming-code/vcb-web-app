# The single Express API (api/ at the repo root), NOT Supabase.
# The browser never talks to Supabase directly — see TECH_STACK.md.
#
# Local:      VITE_API_URL=http://localhost:8080
# Deployed:   VITE_API_URL=https://<your-render-service>.onrender.com
VITE_API_URL=http://localhost:8080
